<h1>Users</h1>

<table class="table">
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.full_name, user_path(user) %></td>
      <td><%= user.email %></td>
    </tr>
  <% end %>
  </tbody>
</table>
